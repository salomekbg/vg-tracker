<!DOCTYPE html>
<html>
  <head>
    <title>VGTracker</title>
    <%= csrf_meta_tags %>
    <script src="https://use.fontawesome.com/1827ac8642.js"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="header">
        <div class="logo"><a href="/">VG Tracker</a></div>
        <div class="nav_buttons">
          <% if session[:user_id] && session[:user_id] == @current_user.id %>
            <%= link_to 'Search for Games', '/games', method: 'get', class: 'search_top' %>
            <%= link_to 'Sign Out', '/signout', method: 'delete', class: 'signout_top'%>
          <% else %>
            <div>
              <%= link_to 'Sign Up', '/signup', method: 'get', class: 'signup_top' %>
              <%= link_to 'Sign In', '/signin', method: 'get', class: 'signin_top' %>
            </div>
          <% end %>
        </div >
    </div>

    <%= yield %>

  </body>
</html>
